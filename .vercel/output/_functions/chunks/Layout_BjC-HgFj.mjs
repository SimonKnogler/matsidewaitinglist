import { e as createComponent, r as renderTemplate, g as addAttribute, k as renderComponent, n as renderHead, o as renderSlot, h as createAstro } from './astro/server_DvjbA6nf.mjs';
import 'piccolore';
/* empty css                         */
import 'clsx';

var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(raw || cooked.slice()) }));
var _b;
const $$Analytics = createComponent(($$result, $$props, $$slots) => {
  const plausibleDomain = undefined                                       ;
  return renderTemplate(_b || (_b = __template(["", "<!-- Custom event tracking --><script>\n  // Track scroll depth\n  let maxScroll = 0;\n  const scrollMilestones = [25, 50, 75, 100];\n  const trackedMilestones = new Set();\n  \n  window.addEventListener('scroll', () => {\n    const scrollPercent = Math.round(\n      (window.scrollY / (document.documentElement.scrollHeight - window.innerHeight)) * 100\n    );\n    \n    if (scrollPercent > maxScroll) {\n      maxScroll = scrollPercent;\n      \n      scrollMilestones.forEach(milestone => {\n        if (scrollPercent >= milestone && !trackedMilestones.has(milestone)) {\n          trackedMilestones.add(milestone);\n          if (window.plausible) {\n            window.plausible('Scroll Depth', { props: { depth: `${milestone}%` } });\n          }\n        }\n      });\n    }\n  });\n  \n  // Track time on page\n  let startTime = Date.now();\n  const timeThresholds = [30, 60, 120, 300]; // seconds\n  const trackedTimes = new Set();\n  \n  setInterval(() => {\n    const timeOnPage = Math.floor((Date.now() - startTime) / 1000);\n    \n    timeThresholds.forEach(threshold => {\n      if (timeOnPage >= threshold && !trackedTimes.has(threshold)) {\n        trackedTimes.add(threshold);\n        if (window.plausible) {\n          window.plausible('Time on Page', { props: { seconds: `${threshold}s` } });\n        }\n      }\n    });\n  }, 5000);\n  \n  // Track CTA clicks\n  document.addEventListener('click', (e) => {\n    const target = e.target;\n    if (target.matches('button[type=\"submit\"]') || target.closest('button[type=\"submit\"]')) {\n      const form = target.closest('form');\n      if (form && window.plausible) {\n        const formId = form.id || 'unknown';\n        window.plausible('Form Submit Attempt', { props: { form: formId } });\n      }\n    }\n  });\n</script>"], ["", "<!-- Custom event tracking --><script>\n  // Track scroll depth\n  let maxScroll = 0;\n  const scrollMilestones = [25, 50, 75, 100];\n  const trackedMilestones = new Set();\n  \n  window.addEventListener('scroll', () => {\n    const scrollPercent = Math.round(\n      (window.scrollY / (document.documentElement.scrollHeight - window.innerHeight)) * 100\n    );\n    \n    if (scrollPercent > maxScroll) {\n      maxScroll = scrollPercent;\n      \n      scrollMilestones.forEach(milestone => {\n        if (scrollPercent >= milestone && !trackedMilestones.has(milestone)) {\n          trackedMilestones.add(milestone);\n          if (window.plausible) {\n            window.plausible('Scroll Depth', { props: { depth: \\`\\${milestone}%\\` } });\n          }\n        }\n      });\n    }\n  });\n  \n  // Track time on page\n  let startTime = Date.now();\n  const timeThresholds = [30, 60, 120, 300]; // seconds\n  const trackedTimes = new Set();\n  \n  setInterval(() => {\n    const timeOnPage = Math.floor((Date.now() - startTime) / 1000);\n    \n    timeThresholds.forEach(threshold => {\n      if (timeOnPage >= threshold && !trackedTimes.has(threshold)) {\n        trackedTimes.add(threshold);\n        if (window.plausible) {\n          window.plausible('Time on Page', { props: { seconds: \\`\\${threshold}s\\` } });\n        }\n      }\n    });\n  }, 5000);\n  \n  // Track CTA clicks\n  document.addEventListener('click', (e) => {\n    const target = e.target;\n    if (target.matches('button[type=\"submit\"]') || target.closest('button[type=\"submit\"]')) {\n      const form = target.closest('form');\n      if (form && window.plausible) {\n        const formId = form.id || 'unknown';\n        window.plausible('Form Submit Attempt', { props: { form: formId } });\n      }\n    }\n  });\n</script>"])), plausibleDomain);
}, "/Users/simonknogler/Desktop/SportApp-Landing/src/components/Analytics.astro", void 0);

const $$Astro = createAstro();
const $$Layout = createComponent(($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro, $$props, $$slots);
  Astro2.self = $$Layout;
  const { title, description = "Algorithm-powered training for martial artists. Stop forgetting what you learn on the mat." } = Astro2.props;
  return renderTemplate`<html lang="en"> <head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><meta name="description"${addAttribute(description, "content")}><meta name="theme-color" content="#0a0a0b"><!-- Open Graph --><meta property="og:title"${addAttribute(title, "content")}><meta property="og:description"${addAttribute(description, "content")}><meta property="og:type" content="website"><meta property="og:image" content="/og-image.png"><!-- Twitter --><meta name="twitter:card" content="summary_large_image"><meta name="twitter:title"${addAttribute(title, "content")}><meta name="twitter:description"${addAttribute(description, "content")}><!-- Fonts --><link rel="preconnect" href="https://fonts.googleapis.com"><link rel="preconnect" href="https://fonts.gstatic.com" crossorigin><link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Space+Grotesk:wght@500;600;700&display=swap" rel="stylesheet"><link rel="icon" type="image/svg+xml" href="/favicon.svg"><title>${title}</title>${renderComponent($$result, "Analytics", $$Analytics, {})}${renderHead()}</head> <body class="min-h-screen bg-[#0a0a0b] text-white antialiased"> ${renderSlot($$result, $$slots["default"])} </body></html>`;
}, "/Users/simonknogler/Desktop/SportApp-Landing/src/layouts/Layout.astro", void 0);

export { $$Layout as $ };
